spring:
  application:
    name: EndlessWorkshop

  datasource:
    url: jdbc:postgresql://<your-neon-dev-host>:5432/<your-neon-dev-db>
    username: ${NEON_DB_USER}
    password: ${NEON_DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        jdbc:
          batch_size: 50
          order_inserts: true
          order_updates: true
          batch_versioned_data: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    locations:
      - classpath:db/migration/common
      - classpath:db/migration/staging

  web:
    resources:
      cache:
        period: 259200
      static-locations:
        - classpath:/static/

logging:
  level:
    org.springframework.boot.autoconfigure: warn
    org.hibernate.SQL: WARN
    org.hibernate.type.descriptor.sql.BasicBinder: WARN

seeders:
  enabled: false
  techPhase3:
    enabled: false
