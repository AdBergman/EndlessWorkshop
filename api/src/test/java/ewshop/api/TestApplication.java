package ewshop.api;

import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * This empty class serves as a lightweight, test-specific entry point for
 * Spring Boot tests within the 'api' module. It allows @SpringBootTest and
 * related test slices like @WebMvcTest to find a valid configuration without
 * needing to depend on the real Application in the 'app' module.
 */
@SpringBootApplication
public class TestApplication {
}
