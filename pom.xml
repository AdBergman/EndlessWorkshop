<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.5</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>
    <groupId>com.endlessworkshop</groupId>
    <artifactId>ewshop</artifactId> <!-- Renamed from ewbackend -->
    <version>0.0.1-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>EndlessWorkshop</name>
    <description>EndlessWorkshop Parent Project</description>

    <!-- Define modules -->
    <modules>
        <module>domain</module>
        <module>facade</module>
        <module>api</module>
        <module>infrastructure</module>
    </modules>

    <properties>
        <java.version>24</java.version>
    </properties>

    <!-- Common dependencies for all modules can go here -->
    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Added H2 for in-memory testing -->
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

</project>
