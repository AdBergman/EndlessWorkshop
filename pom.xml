<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.5</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>
    <groupId>com.endlessworkshop</groupId>
    <artifactId>ewbackend</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>pom</packaging> <!-- Changed to pom for multi-module project -->
    <name>EndlessWorkshop</name>
    <description>EndlessWorkshop Parent Project</description>

    <!-- Define modules -->
    <modules>
        <module>domain</module>
        <module>facade</module>
        <module>api</module>
    </modules>

    <properties>
        <java.version>24</java.version>
    </properties>

    <!-- Common dependencies for all modules can go here -->
    <dependencies>
        <!-- Runtime dependencies have been moved to the api module -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

</project>
